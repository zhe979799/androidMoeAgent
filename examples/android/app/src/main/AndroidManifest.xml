<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- The downloader needs INTERNET. Inference is entirely on-device — nothing about a prompt
         or answer leaves the phone. The optional foreground Network analysis mode also makes only
         the user-visible, bounded diagnostic requests described in examples/android/README.md. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Foreground Network analysis reads the active link and DNS metadata through
         ConnectivityManager. It cannot change a network or obtain Wi-Fi credentials. -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- No broad storage permission here: models arrive through the in-app URL downloader or
         the system file picker, both writing to the app-internal models dir (filesDir, a real
         filesystem where O_DIRECT works). The dev flavor overlay (src/dev/AndroidManifest.xml)
         adds all-files access to also read adb-pushed models in place. -->

    <!-- appCategory="game" is a performance decision, not a claim about what this app is. Vendor
         performance layers read it: on the OxygenOS test device it moves the app onto the boosted
         path, where the foreground CPU ceiling goes from 1.9/1.65 GHz to the hardware maximum of
         3.32/3.80 GHz — measured before and after, with the app in the foreground. Decode is the
         most CPU-hungry thing a phone can do outside a game, so the boosted path is the right one.
         It cannot be a runtime setting: a manifest attribute is fixed at install time, and the
         only lever would be a per-flavor manifest. The effect is the vendor's, not Android's:
         neutral on stock builds, and Samsung's game service has historically THROTTLED what it
         classifies this way, so treat any per-device number as a measurement rather than a rule. -->
    <application
        android:allowBackup="false"
        android:appCategory="game"
        android:extractNativeLibs="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.BigMoeOnEdge">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".RunService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- WorkManager runs DownloadWorker's foreground transfer through its own
             SystemForegroundService, which it declares without a foregroundServiceType. On API 34+
             the type passed to startForeground (dataSync) must be a subset of the type on the
             <service> element, so merge it in here — without this the download crashes with
             "foregroundServiceType 0x1 is not a subset of 0x0". -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />
        <!-- Hands a metrics CSV or agent log to another app (Drive, Mail, Files) as a content:// URI with a
             one-shot read grant. A file:// URI would be refused outright since Android 7, and the
             alternative — telling the user to go find the file — is not a feature. Scoped to the
             app's own external files dir; it exposes nothing else. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
